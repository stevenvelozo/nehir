---
"nehir": minor

---

Overlays register into the Deck (Cmd-D) as an 'Extensions' group via shell.overlay.osd — a keyed tile with a live status line that summons the overlay (Cmd-D then key). Polish: the title-bar close button no longer shows a focus box; the Desktop example uses Ctrl-Opt-Cmd-O.
