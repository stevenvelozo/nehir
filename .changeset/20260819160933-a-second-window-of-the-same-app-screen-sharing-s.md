---
"nehir": patch

---

A second window of the same app (Screen Sharing sessions, extra Terminals/browsers) no longer collapses into one half-height column after a restart — each window keeps its own lane
