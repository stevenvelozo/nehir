---
"nehir": minor

---

The Commandlets palette gains a Nehir group (⌘D → x → n) with one-key diagnostics helpers: start and stop extra trace capture, and capture a trace snapshot plus current window state — run in the inherent terminal, handy for grabbing a repro when a window-management glitch happens.
