---
"nehir": minor

---

Add an Advanced Internals pane (⌘D → A) showing each window's engine runtime state with per-row reset, plus a 'nehirctl command debug reset-focused-window-runtime' escape hatch
