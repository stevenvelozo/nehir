---
"nehir": minor

---

Side-by-side multi-monitor now works with a display on either side: scrolling a column toward the shared edge keeps it flush and visible instead of vanishing, and the 'unsupported display arrangement' warning is removed.
